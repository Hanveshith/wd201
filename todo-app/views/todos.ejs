<<<<<<< HEAD
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <link href="./css/styles.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        var token = document
            .querySelector('meta[name="csrf-token"]')
            .getAttribute("content");
        // console.log(Token);
        function UpdateTodo(id,status) {
            fetch(`/todos/${id}`, {
                method: "put",
                headers: { "content-Type": "application/json",},
                body: JSON.stringify({
                    completed: status,
                    "_csrf": token
                })
            })
                .then((res) => {
                    if (res.ok) {
                        window.location.reload();
                    }
                })
                .catch((err) => console.log(err));
        }

        function DeleteTodo(id) {
            fetch(`/todos/${id}`, {
                method: "delete",
                headers: { "content-Type": "application/json", },
                body: JSON.stringify({
                    "_csrf": token
                })
            })
                .then((res) => {
                    if (res.ok) {
                        window.location.reload();
                    }
                })
                .catch((err) => console.log(err));
        }
    </script>
    <title>TodoList</title>
</head>

<body>
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
            <%- include('header.ejs') %>
            <a href="/signout">SignOut</a>
                <form action="/todos" method="post">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                    <div class="flex py-5 gap-2">
                        <div class="flex-auto">
                            <input class="border border-gray-300 text-gray-900 w-full rounded p-2 text-sm" name="title"
                                type="text" placeholder="Enter Todo" required>
                        </div>
                        <div class="flex-auto">
                            <input class="border border-gray-300 text-gray-900 w-full rounded p-2 text-sm leading-4"
                                name="dueDate" type="date" required>
                        </div>
                        <div class="flex-none">
                            <button class="bg-green-600 text-white px-5 py-1.5 rounded font-medium mr-2"
                                type="submit">Add</button>
                        </div>
                    </div>
                </form>

                <%- include('todo.ejs',{ "todotype" : "Overdue" , "id" : "count-overdue" , "todos" : overdueTodos }) %>

                <%- include('todo.ejs',{ "todotype" : "Due Today" , "id" : "count-due-today" , "todos" : dueTodayTodos }) %>

                <%- include('todo.ejs',{ "todotype" : "Due Later" , "id" : "count-due-later" , "todos" : dueLaterTodos }) %>

                <%- include('todo.ejs',{ "todotype" : "Completed Items" , "id" : "count-completed-items" , "todos" : completedTodos }) %>
        </div>
    </div>

    <!-- 
    <%- include('footer.ejs') %> -->
</body>

</html>
=======
<div>
  <div class="flex">
    <h5><%= todotype %></h5>  
    <span id="<%= id %>" class="ml-2 px-2 bg-gray-100 rounded"><%= todos.length %></span>
  </div>
  <ul class="list-none">
    <% for (var i = 0; i< todos.length; i++) { %>
      <li class="TodoItem">
          <div class="flex items-center w-fit my-2 px-2 py-1 rounded hover:bg-purple-50">
              <input class="w-4 h-4 text-blue-600 bg-gray-100 rounded border" type="checkbox" <%= todos[i].completed ? "checked" : "" %> onclick="UpdateTodo(<%= todos[i].id %>,<%= todos[i].completed %>)" id="todo-checkbox-<%= todos[i].id%>">
                  <label for = "todo-checkbox-<%= todos[i].id%>" class="ml-2 text-gray-600">
                    <%= todos[i].title %>
                  </label>
                  <a href="#" class="hidden trash-icon ml-2" onclick="DeleteTodo(<%= todos[i].id %>)">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M14.74 9l-.346 9m-4.788 0L9.26 9m9.968-3.21c.342.052.682.107 1.022.166m-1.022-.165L18.16 19.673a2.25 2.25 0 01-2.244 2.077H8.084a2.25 2.25 0 01-2.244-2.077L4.772 5.79m14.456 0a48.108 48.108 0 00-3.478-.397m-12 .562c.34-.059.68-.114 1.022-.165m0 0a48.11 48.11 0 013.478-.397m7.5 0v-.916c0-1.18-.91-2.164-2.09-2.201a51.964 51.964 0 00-3.32 0c-1.18.037-2.09 1.022-2.09 2.201v.916m7.5 0a48.667 48.667 0 00-7.5 0" />
                    </svg>
                  </a>  
          </div>
      </li>
    <% } %>
  </ul>
</div>
>>>>>>> e0a45502d29c31042a7767f879e8da871ee9f676
